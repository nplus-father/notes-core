---
interface Props {
  status: "draft" | "studied" | "reviewed";
}
const { status } = Astro.props;
const label: Record<Props["status"], string> = {
  draft: "Draft",
  studied: "Studied",
  reviewed: "Reviewed",
};
---

<span class={`badge status-${status}`}>{label[status]}</span>
